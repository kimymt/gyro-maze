# スマホの開始ボタン表示改善案

2026-09-22。A案の実装を承認済み。ボタン文言はSTART。
対象: https://gyro.mymt.casa/

## 観察

添付の実機画像では開始ボタンの下部が画面外に出ている。BEST記録を持つステージでは記録が2行目に入り、一覧の行高が増える。現在のスマホ用プレビューは最小260px（狭い幅では220px）、flex-shrink:0。ヘッダー、一覧、操作選択、余白、安全領域の合計が表示領域を超えた際に開始ボタンが下へ押し出される。縦長という縦横比だけでは利用可能な高さは判定できない。

## A: 開始ボタンだけ下部に常設（推奨）

スマホのステージ選択画面で、開始ボタン専用の領域を画面下部に固定する。ホームインジケーターの安全領域と上下の余白を確保する。本文には固定領域の高さ分の下余白を設け、最後の操作項目・インストール案内までスクロールできるようにする。既存のボタンを移動し、複製しない。ダイアログ表示中とプレイ中は表示しない。

長所: BEST記録数、一覧の増加、文字サイズによる高さ変化に影響されず開始できる。
短所: 下部の表示領域を常時使う。一覧と操作方法は必要に応じてスクロールする。

## B: 画面の高さに合わせて3Dプレビューを縮める

幅だけでなく使用可能な高さに応じてプレビューと余白を縮め、開始ボタンを通常の文書フロー内に残す。
長所: 現在の構成を保ち、全体を一画面に収めやすい。
短所: 記録が全件に付く場合や文字拡大では収まらない。プレビューが小さくなるため単独では確実な対策にならない。

## C: ステージ一覧を6件ずつ切り替える

01〜06と07〜12をページ切替で表示し、一覧を3行にする。
長所: 3D表示と操作領域を大きく保てる。
短所: 全12ステージを同時に比較できず、選択操作が増える。

## 推奨と確認項目

Aを採用する。Bは実機でプレビュー領域が不足した場合に追加検討する。文字やタップ領域の縮小で収めない。

検証は記録なし・一部あり・全12件あり、ライト・ダーク、Safariのツールバー表示・非表示、ホーム画面起動、文字拡大、縦横切替を対象にする。開始ボタン全体が安全領域の上に表示され、操作方法と案内の末尾が固定領域の裏に隠れず到達できることを確認する。未解放ステージからの案内とプレイ開始・終了時の表示切替も検証する。ブラウザの自動検証とiPhone実機確認は分けて報告する。
