# v0.6.0 検証記録（旧方式）

v0.7.0でユーザー指示により自動入金確認を廃止し、Lightningも自己申告へ変更。この文書は旧方式の検証履歴であり、現行仕様ではない。実機で送金後の未解放が報告され、請求は一致したが入金確認記録は存在しなかった。通知不達・拒否・保存失敗のどれかは特定できていない。

2026-09-21。12コース・支援による一括解放。

- TypeScript: クライアント・Workerとも通過。Vite/SW生成通過。
- Vitest: 全177件通過。その後、API入力サイズの防御テストを追加し、決済4件を再実行して通過。合計178項目。
- Playwright: 38件の回帰検査通過。その後、06→07・復元を追加して影響範囲13件を再実行、通過。最後に描画障害と入金応答の競合を追加し、解放機能7件すべて通過。全体で41項目。
- ローカルCloudflare実行環境: 実際のWebSocket→署名検証→KV→復元照会を通過。偽造通知は拒否、正しいテスト署名と重複通知は受理。テスト用請求データのみ、資金移動なし。
- モバイル幅393pxでステージ12、解放、Lightning入力の表示を確認。既存レイアウト検査で320px・横向きも通過。
- 全12コースの重力のみでの走破、合成端末入力の走破・ゴール停止、球の床接触・端点の保持を通過。

11: 地形一辺42、道のり655.01。12: 一辺48、道のり758.53。11・12はルート曲線の距離計算の解像度を増やし、既存の最大サンプル間隔0.051を維持。接触検査はコース長に応じて処理が増えるため、その検査の実行上限を5秒から15秒へ変更した。接触誤差・移動量の許容値は変更していない。

暗号ライブラリはWorkerだけに含む。クライアントの解放画面8.74kB、QR23.46kBは遅延読み込み。初回SWキャッシュには含むため保存時の通信は増える。iPhone実機での起動時間・処理負荷は未計測。

受取サービスへの通常請求とNIP-57請求の発行を確認したが、どちらも未払い。実送金によりWoSが通知を配送するところからの確認、iPhoneのウォレット往復、実際の支払い後の復元は未実施。手順は DEVICE_TESTS.md。

本番の初回請求検査で、Cloudflare fetchが `redirect: error` を受け付けないことを検出。`manual` に変更し、3xxを含む非成功応答を拒否する方式へ修正。固定受取先以外へリダイレクトを追従しない条件は維持。
