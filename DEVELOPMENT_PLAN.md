# GYRO 制作開発計画 — 落ちない自然の立体迷路

更新: 2026-09-13。ユーザーの実機フィードバックを受け、落下のある初期ステージを置き換える。

## 遊び方

- 1本指で立体を360度回転。2本指のひねりとピンチに対応。
- 開始前も球を転がして試せる。タイムと記録は開始後だけ計測。
- 球を常に床に接触させ、見えない側面・端の境界でコース内に保つ。落下失敗・自動で中継点に戻す処理・落下回数表示を廃止。
- 通路に沿って金色の中継点を通過し、緑のゴールで球を止める。時間制限なし。
- 視点リセットは中継点へ戻す明示操作として残す。

## ステージ

| レベル | 名前 | 主な素材 | 地形の一辺 | 構造 |
| --- | --- | --- | --- | --- |
| 01 | 木漏れ日の箱庭 | 木・苔 | 4.4 | 外周から内部へ入り、正面へ出る |
| 02 | 土の中の回廊 | 土・石・苔 | 7.2 | 外周と2つの内部回廊を巡る |
| 03 | 水を抱く大地 | 水・石・土・苔 | 10.8 | 大きな地形の内部・外周・上面を巡る |

球の大きさは共通。上位ステージでは地形の実寸と道のりが増える。形は立方体を基調とし、木目、土の粒、苔、水面の小さなテクスチャをローカル生成する。外部画像・モデル・フォントの取得は行わない。

## 物理と見え方

- Rapierで120Hzの重力・速度・回転を計算。球が休止していても重力方向の変更で再開する。
- 床と見えない境界は、通路中心線から連続的に計算する。タイル同士の衝突の継ぎ目をなくし、毎ステップ球の中心を許容領域内へ補正する。床から離れる・床へ沈む速度成分を除去し、床に沿って転がす。側面と端では外へ向かう速度成分を除去する。
- 面の切り替わりでは通路に沿って床の向きを連続的に運び、面ごとの向きへ徐々に補間する。接線と平行になる法線を無理に使わず、急な反転を防ぐ。
- 通路は細かな等距離サンプルから一続きの曲面として描画する。描画と床判定で同じ補間フレームを使い、接続面で段差を作らない。
- スタート・ゴールの床をマーカーから外側へ延長し、球の中心はマーカー位置で止める。側面も球の半径分を内側に制限し、球全体を床の内側に保つ。
- 球の速度を制限し、急な操作でも速すぎないようにする。見えない壁のメッシュは生成しない。
- 地形と通路は素材別・領域別にまとめて描画し、ステージ大型化時の描画回数を抑える。
- 内部へ入ったら地形を薄く表示。球を遮る通路も透過し、進路を追えるようにする。
- 全体表示は大きさに合わせて調整。「球に寄る」またはピンチ拡大では球を中心に見る。
- バックグラウンドで停止。停止時間の追いつき計算はしない。

## 保存と公開

旧ステージの記録は新ステージの記録と分け、画質設定は引き継ぐ。新ステージも自己ベストを端末保存する。

Cloudflare Workers Static Assetsの既存URLへ反映する。全素材保存、失敗時の再試行、オフライン再起動、選択画面からの更新は維持する。ゲーム中の強制更新はしない。

公開URL: https://gyro-maze.kei1127miyamoto.workers.dev

## 検証

- 重力操作だけで全コースを走破し、中継点と終点へ到達できること。
- ランダムな急回転、逆さま、強い初速でも球が許容領域を越えないこと。
- 休止後に傾け、指を離しても球が動くこと。選択画面とゲーム画面の両方で画像比較する。
- 全コースに外周区間と内部区間があり、大きさと道のりが順に増すこと。
- スマートフォン幅、横持ち、2本指操作、記録破損、オフライン再起動、取得失敗からの復旧、更新待機を確認する。
- iPhone実機の操作感・見やすさ・10分継続性能は DEVICE_TESTS.md に別途記録。デスクトップ試験を実機保証として扱わない。
