# 12コース化と支援によるアクセス解放

2026-09-21。基準 v0.5.1、実装 v0.6.0。

## 確定した仕様

- 11は巨木の根道、12は峰を渡る道。地形の一辺は42、48、道のりは655.01、758.53。10より順に大きい。
- 01〜06は無料。07〜12は、SNSシェア・任意の募金箱や団体への寄付・開発者へのLightning送金のいずれか一つで一括解放。
- シェアと寄付は自己申告。Lightningだけ自動確認。
- 受取先 `garbledaction736@walletofsatoshi.com`、金額は1 sat以上の任意額（受取サービス上限100,000,000 sat）。
- 指操作を既定に保ち、端末傾き操作、落下防止、床接触、旧BEST、オフラインプレイを維持。

## 入金確認方式

指定先のLNURL-payメタデータは1 sat以上・NIP-57対応を返す。通常の未払いテスト請求にはLUD-21の照会URLがなかった。追加の未払いテストで、NIP-57要求とゲーム専用通知先を指定した請求発行を確認した。送金は行っていない。

既存Cloudflare Workerに請求発行・状態照会・WebSocket通知受信を追加。固定HTTPS受取先だけを呼び、NIP-57署名者・請求書・要求JSON・金額・description hashを照合。検証済み通知をKVに記録してから解放する。公開リレーは使わない。請求書パーサー自体はECDSA署名を検証しないため、固定HTTPS受取先と署名付き受取通知を信頼境界とする。

入金記録とランダム復元トークンのハッシュは期限なし保存。復元コードは端末側に保管し、共有URLや通知には含めない。重複通知も同じ請求の解放として扱う。期限切れのみで入金済み扱いにしない。照会失敗時は再送金を促さず、復元コードと元の請求を維持する。

## 画面と保存

12コースを2列で表示。未解放コースもプレビュー可能。開始・再挑戦・次のコースは共通の判定を使う。06から07も解放画面へ進む。既存07〜10のBESTは保持する。

SNS投稿や寄付先への送金は利用者自身が行う。共有画面の成功・キャンセル、リンクコピー、QR表示だけでは解放しない。寄付は選んだ先へ直接行い、GYROは預からない。

解放は端末内に保存。保存不可ならセッション中だけ有効と表示。Lightningは復元コードで別端末にも復元可能。Safariとホーム画面アプリ間の保存共有は保証しない。自己申告の解放はデータ削除後に再申告が必要。

全コースをオフライン配信するため、コード改変を防ぐDRMではない。支援を促す仕組みとして実装。

## 起動・検証

起動時の決済通信なし。解放画面・QRは遅延読み込み、署名処理はサーバーのみ。初回オフライン保存には追加モジュール分の通信が増える。

自動検証: 全コースの重力走破・床接触・端点・ルート差異、無料範囲、自己申告、共有キャンセル、未払い、通信失敗、復元、偽造署名、請求不一致、重複通知、保存拒否、オフライン。

残る実機検証: iPhone Safari・ホーム画面アプリのウォレット往復、実送金のWoS通知到達、支払い後の復元。実際の資金移動はユーザーの別途明示的な許可なしに行わない。

## 確認した一次資料

- 受取設定: https://walletofsatoshi.com/.well-known/lnurlp/garbledaction736
- NIP-57: https://github.com/nostr-protocol/nips/blob/master/57.md
- LUD-21: https://github.com/lnurl/luds/blob/luds/21.md
- Web Share: https://www.w3.org/TR/web-share/
- Cloudflare WebSocket: https://developers.cloudflare.com/workers/runtime-apis/websockets/

上記は2026-09-21にHTTP応答と内容を確認済み。
