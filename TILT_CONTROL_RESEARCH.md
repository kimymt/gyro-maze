# 端末の傾き操作の追加調査

調査日: 2026-09-14。基準: v0.2.6 / `98a96d7ae51f4e238e70eb51521970e5425d459a`。`origin/main` と一致し、開始時の作業ツリーはcleanだった。ユーザーから現行版の実機確認完了の報告あり。追加するセンサー操作の実機確認とは区別する。

状態: 以下は実装前の調査記録。ユーザー承認後にv0.3.0として実装した。最終仕様・検証結果は `research/tilt-implementation-20260920.md` を参照。新しい端末操作の実機確認は未実施。

## 結論

追加は可能。指操作を毎回の既定とし、ユーザーが選んだときだけ端末の傾き操作を有効にする構成を推奨する。既存の床拘束・重力計算を維持し、回転入力の入口を増やす。

2026-09-14追記: ユーザーの「ステージ03側を変える方が実装楽では」という提案を受け、推奨を変更する。前案は現行コースの維持を前提にしすぎていた。

**ステージ03の大きな登りを見直し、端末の傾きに迷路の角度が直接追従する方式を第一候補とする。** 元の持ち方に戻すと迷路も基準姿勢へ戻る。回転速度を積分する方式に比べ、保持角度・連続回転・停止操作の説明を減らせる。指操作は既定のまま残す。

数値上の最小比較として、`src/levels.ts:126–128`にあるステージ03だけの末尾4ガイドを除いた一時案を調べた。前進方向の重力を作るための最大必要角は85.73°から7.63°へ低下。道のりは75.33から55.72となり、02の38.63より長い。上面・外面・内部の基本経路と大きい地形は維持できる。

これは採用する最終マップではなく、上りを取り除く効果だけの比較。ゴールの見やすさ、停止、生成曲線の局所的な勾配と自己接近を再設計・検証する。急な下りで反対向きの重力を作るための角度は約62.39°のままで、前進可能性の改善だけを操作性の保証にしない。必要に応じて下りを緩めるか、自動で通過する区間として許容できるかを実機で確認する。

入力実装は単純になるが、権限、基準合わせ、画面角度補正、手ぶれ抑制、背景停止、起動との分離は引き続き必要。コースを短くしてもキャッシュ・描画負荷の改善は自動ではなく、仮案では地形ブロックが486から531へ増えた。新方式の起動時間・発熱は実装後に比較する。

マップを変える場合は、旧03のBESTを新コースの記録として比較しない。コースの版を区別し、既存記録の扱いも実装対象にする。

以下の85.73°・9.30の解析表は、調査時点の変更前マップに対する根拠。

## 現行コードから確認したこと

| 箇所 | 現状 | 追加時の意味 |
| --- | --- | --- |
| `src/main.ts:156–165` | 3Dを作り、Rapier準備を待って開始可能にする | 許可要求・初回センサー値・傾きモジュール取得をbootの待機条件に加えない |
| `src/input.ts:11–22` / `src/scene.ts:81` | 指の差分をQuaternionにし、カメラ座標から世界座標へ回転を変換 | センサー側も同じ`Scene.rotate()`経路へ入れる |
| `src/state.ts:3` / `src/physics.ts:18–22` | 迷路姿勢の逆回転からローカル重力を計算 | 見た目と物理へ別々の姿勢を渡さない |
| `src/physics.ts:24–45` | 球を床へ戻し、側面・端を制限。速度上限2.6 | 有限で正規化した回転を渡す限り、既存の拘束を再利用できる |
| `src/physics.ts:20` / `src/main.ts:152` | 微小な重力差でも球を起こす。静止時は描画を省略 | センサーノイズの直結は静止・省電力を壊す |
| `src/main.ts:46–48` | `resetBall()`は球を中継点へ戻す | センサーの中立位置設定に流用すると意図せず球が移動する |
| `scripts/build-sw.mjs:4–8` / `src/sw.template.js:4–7` | distの全素材を初回にキャッシュ | 遅延importにしても分割JSはオフライン保存時に取得される |

既存の床拘束・全ルート走破テスト19件が合格した。任意の重力方向で通路を進めることの確認であり、端末の傾きによる操作性・完走の保証ではない。

## 操作方式の比較

| 方式 | 起きる問題・条件 | 判断 |
| --- | --- | --- |
| 端末角度 → 迷路の絶対角度 | 小角度の範囲では前進方向の重力を作れない登りがある。角度範囲を広げると画面を見づらい持ち方になり、倍率を上げると手ぶれも増える | 03のコース変更を前提に第一候補 |
| 傾き量 → 迷路の回転速度 | 小さな傾きを保持して任意角へ回せる。角速度上限・中立位置・停止操作の説明が必要 | 現行コースを維持する場合の代案 |
| 端末センサー → 重力だけ変更 | 迷路の見た目と力が一致しない。内部通路を見るための回転も別途必要 | 採らない |
| 絶対傾き＋指で大きく回す | 傾きは微調整として自然。端末操作だけで全体を回す方式にはならない | 別案として成立 |

### 小角度への直接対応が不十分な根拠

実ルートの接線に沿う重力の最大成分を解析した。条件は「開始時の迷路姿勢を基準に、結果のローカル重力が半角30°の円錐内で動く」。端末の各Euler軸を±30°に制限する条件とは異なる。

| ステージ | 最も厳しい地点で前進方向の重力を作るために必要な方向変更 | 半角30°では前進加速度を作れない連続区間 |
| --- | ---: | ---: |
| 01 | 7.08°より大きい変更 | 0 |
| 02 | 7.43°より大きい変更 | 0 |
| 03 | 85.73°より大きい変更 | 長さ9.30、基準重力に逆らう上昇7.65 |

距離はゲーム内の単位。03の対象はルート点1212〜1400。01・02の0は完走保証ではなく、この必要条件で阻まれる区間がないという意味。03についても限定傾きモードの完走シミュレーションではないため、勢い・通路幅を含む厳密な到達不能の証明としては扱わない。操作方式の設計不足を示す解析である。

計算では、単位基準重力`g0`、単位接線`t`、円錐半角`θ`、`d=dot(g0,t)`から、接線が円錐外なら重力投影の上限を`9.81 * (cos(θ)*d + sin(θ)*sqrt(1-d*d))`として判定した。円錐内なら上限は9.81。identity基準でも同じ問題があった。

再現:

```sh
node --experimental-strip-types scripts/research-tilt-route.mjs
```

結果は`research/tilt-route-20260914.json`。調査時のv0.2.6でNode v26.7.0を使って再実行し、保存したJSONとの一致を確認した。スクリプトは実行時のチェックアウトを解析するため、新03へ変更後の結果はこの履歴JSONとは異なる。

## 起動時間と負荷

### 指操作の起動を守る構造

1. 初期モードは指操作。前回端末操作を使っていても、起動時に許可要求・listener登録・センサー準備を行わない。
2. 切替ボタンから許可要求を開始する。センサーの初期化はbootと独立させる。既存の3DやPhysicsを作り直さない。
3. 大きな外部ライブラリ・新しいWASM・Workerは追加せず、既存Three.jsの計算を使う小さな入力アダプタとする。
4. dynamic importは未使用コードの実行を遅らせる手段として比較する。現在の全素材キャッシュでは初回転送量をなくせないため、JSを分割するだけで起動が速くなるとは決めない。
5. 追加分gzip 5KB以内を暫定目標とし、実ビルドで初回JS・全キャッシュ量・評価時間を確認する。これは実装前の目標であり、測定結果ではない。

現行distの実測はmain 586,156B（Node標準gzip 149,171B）、Rapier 2,851,776B（同1,083,792B）。追加コードは未実装なので増加量・起動時間差は未測定。

### 操作中の負荷を増やしすぎない構造

センサーイベント内では最新の数値と時刻だけ保存する。DOM書換え・描画・物理更新をイベントごとに実行しない。既存フレーム処理で入力を取り込み、現在の傾きから目標姿勢を求める。第一候補ではゲーム内の回転速度を積分しない。センサー専用の描画ループや物理ステップを追加しない。

中立付近には不感帯とヒステリシス、時間に基づく平滑化を入れる。不感帯の内側は出力を厳密に0にして`Scene.rotate()`を呼ばない。初期候補は不感帯1.5〜2°、最大入力角20〜25°。物理へ渡す姿勢の変化も制限して急な持ち替えを処理する。実機で調整する値であり、快適性が確認済みの設定ではない。

センサー利用時の電池・発熱は増え得る。指操作・非表示・一時停止・ヘルプ・結果・選択画面の待機中は購読を停止する。開始直前の短い中立位置取得だけは実施する。現在もrAFと物理更新は存在するため、受け入れ条件は「指操作中に追加センサー処理が走らない」であり、アプリ全体のCPU使用ゼロではない。

## Safari / iOSの条件

モバイルWebKitではDevice Orientation/Motionへのアクセスにユーザー許可が必要。許可要求はモード切替ボタンの同期ハンドラで行い、モジュールのダウンロードを`await`した後まで遅らせない。許可拒否・API未対応・値が取得できない場合は指操作を維持する。[WebKitの方針](https://webkit.org/tracking-prevention/) / [Device Orientation and Motion仕様](https://www.w3.org/TR/orientation-event/)

HTTPS上で実装する。Safari 26.4の公式変更記録でも、センサーのインターフェースをsecure contextだけへ公開する修正が確認できる。公開済みのHTTPS配信元を使い、Safariタブとホーム画面を別々に確認する。許可の保持期間や両者の共有状態を決め打ちしない。[Safari 26.4変更記録](https://developer.apple.com/documentation/safari-release-notes/safari-26_4-release-notes)

姿勢データの座標軸は端末の基準方向に結びつき、表示が横向きになっても自動で画面軸へ変わるわけではない。現在の画面角度を加味してから、ゲームのカメラ軸へ変換する。画面方向固定APIの成功は前提にせず、縦横変更時は停止して中立位置を取り直す。[姿勢の座標系](https://www.w3.org/TR/orientation-event/#model) / [Screen Orientation仕様](https://www.w3.org/TR/screen-orientation/)

### イベントの欠測と入力値

第一候補は`deviceorientation`の姿勢から方位に依存しない2軸の傾きを求める方式。コンパスの絶対方位や生の角速度の長時間積分へ依存させない。Euler角の値を直接引き算せず、重力方向やQuaternionとして連続性を処理する。

ただし、`deviceorientation`は変化を契機に通知するため、一定周期の到着を保証しない。端末を傾けたまま保持しているとき「イベントが500ms来ないので故障」と判断する設計は採らない。継続監視には周期通知の`devicemotion`を組み合わせる案を検証する。必要な許可要求はすべて同じユーザー操作内で先に呼ぶ。代案としてMotionデータから重力方向を推定する場合、端末の並進加速度が混ざる点の評価が必要。生の`accelerationIncludingGravity`をゲームの重力へ直結させない。[イベントモデルと取得値](https://www.w3.org/TR/orientation-event/)

取得の初回タイムアウト、継続監視の停止閾値、両APIが実機で提供する値・間隔は最初の試作で測定する。仕様で読み取れることと、そのiPhoneでの振る舞いを分ける。Safari 27の公式情報から、このゲームのセンサー操作の速度や安定性を保証できる情報は得られていない。[Safari 27公式紹介](https://webkit.org/blog/17967/news-from-wwdc26-webkit-in-safari-27-beta/)

## 破綻を防ぐ条件

| 条件 | 起き得る破綻 | 必要な処理 |
| --- | --- | --- |
| 傾きの小さなノイズ | 静止中も球が起き、画面が揺れ、電池を使う | 不感帯・平滑化・ゼロ出力時の回転呼び出し抑制 |
| モード切替・持ち替え | 迷路姿勢が跳び、球の速度方向が急変 | 一時停止したまま現在の迷路姿勢を維持し、端末側だけ基準合わせ |
| センサーの基準合わせ | 球が勝手に中継点へ戻る | `resetBall()`とは別の処理にする |
| ドラッグ／ピンチ併用 | 指と傾きの回転が加算される | タッチ中はセンサー回転を抑制。再開時の基準を明示して残差を消す |
| 縦横変更 | 操作軸が90°入れ替わる | 画面角度補正、入力履歴消去、停止中の再基準合わせ |
| 許可ダイアログ・ヘルプ・背景化 | 見えない間に球と時計が進む | 物理・計時を止め、センサー購読を解除。自動再開しない |
| 遅れて届く許可・import結果 | 指操作へ戻したのにセンサーが再有効になる | 操作世代番号で古い非同期結果を破棄 |
| null / NaN / 極端な値 | Quaternionが壊れ、物理へ無効値が入る | 有限値・角度・時刻を検証し、採用できない入力を破棄。継続不能なら停止して指操作へ戻す |
| センサー停止・イベントの遅延 | 最後の姿勢が保持され、球がその斜面で進み続ける | 周期の確認できる監視経路を使い、停止を検出したら回転を0にしてゲームを一時停止 |
| 低fps・復帰時の大きなdt | 回転がまとめて適用される | 既存固定ステップと角速度上限を使い、停止中の入力を積み残さない |
| ゴールでの微調整 | 迷路を回しすぎ、球を止められない | 小さな傾きでの低速操作、不感帯、指操作への切替を用意。実際の完走で調整 |

## 実装する場合の順序と採用判定

0. 03の上りを低い位置を巡る経路へ置き換える案を作り、生成後の全ルートで前進・減速・ゴール停止・視認性を検証する。03の記録は変更前と区別する。
1. 開発用の試作でセンサー許可、取得間隔、静止ノイズ、縦横の符号を測定する。Safariとホーム画面の両方で行う。
2. 本体へ依存しない小さな入力アダプタを作り、角度折返し、ノイズ、30/60/120Hz相当入力、欠測、非表示、連続切替を人工データで検証する。
3. 指操作を既定として共通の回転経路へ接続。設定の中立位置取得、タッチ併用、停止復帰を実装する。物理エンジンの閾値を変えてノイズをごまかさない。
4. 同じiPhone・OSビルド・回線・画質・低電力／充電状態で、現行版と試作の起動を交互に各5回測る。初回・キャッシュ済み・オフラインを分け、「最初の表示」「指操作で開始可能」「オフライン保存完了」を記録する。許可ダイアログに人が回答する時間は起動指標へ混ぜない。
5. 指操作で起動した場合、許可ダイアログ0回・センサー購読0・追加入力処理0を確認。起動中央値が`max(100ms, 旧版の5%)`を超えて悪化する場合は再計測・原因調査する。閾値は今回の暫定受け入れ基準。
6. 各ステージ10分、指操作と端末操作のフレーム時間・30fps未満の継続・発熱を比較する。中立保持で不要な回転がなく、平坦な場所では既存のsleep／描画省略を維持する。
7. 3ステージを端末操作主体で完走し、内部通路の視認性、ゴール停止、球の床接触と端点拘束、縦横変更、持ち替え、ロック復帰、許可拒否を確認する。
8. 全素材保存後に機内モードで起動し、指→端末→指の切替を確認。取得失敗や権限拒否でも既存の指操作へ戻れることを確認する。

現時点の判定: 技術的に追加可能。ステージ03のコース変更と角度への直接追従方式による試作を推奨。起動時間・電池・操作難度の合格判定は追加実装と実機比較の後に行う。現行版の実機確認を、新モードの承認や性能検証に代用しない。
